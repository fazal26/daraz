<nav class="navbar navbar-expand-lg navbar-dark bg-dark navbar-inverse no-radius-border">
  <%= link_to "Daraz", products_url, class:"navbar-brand" %>
  <div class="collapse navbar-collapse " id="navbar">
    <% if user_signed_in? %>
      <div class="row">
        <div class="col-sm-10"> 
          <% if !is_admin %>
              <%= link_to "Add Product", new_product_url, class: "btn btn-success" %>
              <%= link_to "Cart", cart_path, class: "btn btn-primary" %>
              <%= link_to "Orders", orders_path, class: "btn btn-primary" %>
          <% end %>
        </div>
        <div class="col-sm-2 row"> 
          <%= render partial: "partials/user_profile" %>
        </div>

          <%= link_to "Out", destroy_user_session_path, method: :delete, class: "btn btn-danger" %>

      </div>
    <% else %>
      <%= link_to "Sign Up", new_user_registration_path %>
    <% end %>
  </div>
</nav>
