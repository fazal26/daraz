<%= render 'new_comment' %>
<div id="commentBox">
  <% comments.each do |comment|%>
    <%= render 'comments/comment_detail' , {product: product, comment:comment, current_user_id: current_user.id}%>
  <% end %>  
</div>